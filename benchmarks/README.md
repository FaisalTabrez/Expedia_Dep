# Benchmarks and validation studies

This directory holds preregistrations, dataset manifests, baseline definitions,
metrics, and raw-result references. It does not contain a claim merely because a
model or visualization exists.

Method-evaluation work is M3. M1 retains only release-integrity and software
verification evidence.
