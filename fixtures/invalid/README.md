# Invalid fixtures

M1 and M2.1 malformed-contract examples live here. Every fixture must be
rejected by the locked contract suite; M2.1 fixtures cover schema and canonical
request semantics without exercising a release adapter or query execution.
