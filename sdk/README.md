# Python SDK

The Python SDK is a typed wrapper over Query Core or its REST transport adapter.
It does not own filtering, ranking, profile compatibility, pagination, or
provenance semantics.

No SDK client behavior is implemented in this skeleton.
