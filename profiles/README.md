# Profile declarations

Profiles make representation, canonicalization, derivation, validation, and
plugin choices explicit and versioned. A profile is data/provenance, not an
implicit runtime default. New models must create new immutable profiles; they
must not overwrite historic vectors or change a release's semantics.
