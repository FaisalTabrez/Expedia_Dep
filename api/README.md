# REST API

REST is a versioned transport adapter over Query Core. It is not an independent
query implementation and never mutates a Published release.

The OpenAPI file is a structural placeholder for `/v1`; routes remain empty
until Query Core contracts and conformance tests exist in M2.
