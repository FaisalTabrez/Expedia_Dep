# Explorer

Explorer is a release-aware client. It presents release identity, provenance,
profile/metric context, evidence status, and derived-artifact lineage without
becoming an alternate source of query semantics.

The Explorer is planned. This skeleton contains no desktop or web application
behavior.
