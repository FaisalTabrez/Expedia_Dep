# Arrow schemas

This directory will contain versioned Arrow schema definitions for in-memory
interchange. They must correspond to the canonical logical contracts in
`../json/` and are not independently authoritative.
